conjunto = {14, 7, 12, 8, 6, 14, 3}
print(f"Tipo do conjunto: {type(conjunto)}") # O tipo do conjunto é exibido como <class 'set'>, indicando que a variável 'conjunto' é do tipo set, 
                                             #que é a estrutura de dados usada para representar conjuntos em Python.

print(conjunto) # O conjunto é exibido como {3, 6, 7, 8, 12, 14}, mostrando os elementos únicos do conjunto. Note que o número 14 aparece apenas uma vez,
                # mesmo que tenha sido adicionado duas vezes, pois os conjuntos não permitem elementos duplicados
                